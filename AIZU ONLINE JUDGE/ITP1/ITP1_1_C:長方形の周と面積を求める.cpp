#include <bits/stdc++.h> 
using namespace std;

int main(){
    // 初期値定義
    int a, b;
    cin >> a >> b;

    // 処理定義
    int men = a * b;
    int syu = (a + b) * 2;

    // 出力定義
    cout << men << " " << syu << endl;
}