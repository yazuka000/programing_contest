/* sample input

*/

#include <bits/stdc++.h> 
using namespace std;

int main() {
    // 初期値定義
    char ch;
    cin >> ch;

    // 処理定義
    ch=toupper(ch);

    // 出力定義
    cout << ch << endl;

    return 0;
}