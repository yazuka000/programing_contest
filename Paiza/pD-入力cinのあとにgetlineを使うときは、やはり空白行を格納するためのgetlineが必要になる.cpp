#include <bits/stdc++.h> 
using namespace std;

int main() {
    // 初期値定義
    int a;
    cin >> a;
    string t, str[a];
    getline(cin,t);

    // 処理定義
    for(int i=0; i<a; i++){
        getline(cin,str[i]);
        cout << str[i] << endl;
    }

    // 出力定義
    
    return 0;
}